#pragma once

/*
#include "GlobalHeader.h"
#include "CustomModel.h"

class CompositeModel
{
private:
	vector<CustomModel*> models;

	const aiScene* scene;
	
public:
	~CompositeModel();

	void init(string modelName);

	void render(Mat4 projectionViewMatrix);
};

*/